/**
 * Polyfills for the application.
 *
 * This project currently does not require additional polyfills. Keep this file
 * present so the Angular build can resolve the entry. Add polyfills here only
 * if supporting older browsers or when a specific polyfill package is needed.
 *
 * Example (uncomment to enable):
 * // import 'zone.js'; // if Zone.js is required by your app
 */
